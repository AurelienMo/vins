{#<div class="modal fade" id="modalLoginForm" tabindex="-1" role="dialog" aria-labelledby="myModalLabel"#}
{#     aria-hidden="true">#}
{#    <div class="modal-dialog" role="document">#}
        <div class="modal-content">
            <div class="modal-header text-center">
                <h4 class="modal-title w-100 font-weight-bold">{{ dto.wine }}</h4>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body mx-3">
                {{ form_start(form, {'attr': {'class': 'addWineCart'}, 'action': path('add_wine_to_card', {'id': dto.wine.id})}) }}
                {% for element in form.elements %}
                    <div class="d-flex justify-content-between align-items-center">
                        <div class="d-flex flex-column">
                            <span>{{ element.vars.data.capacity.type }} {{ element.vars.data.capacity.quantity }}</span>
                            <h3 class="h3">{{ element.vars.data.capacity.unitPrice }} €</h3>
                        </div>
                        <div class="md-form mb-5">
                            {{ form_widget(element.quantity) }}
                        </div>
                    </div>

                {% endfor %}

                <button type="submit" class="btn btn-default">Ajouter au panier</button>
                {{ form_end(form) }}
            </div>
        </div>
{#    </div>#}
{#</div>#}